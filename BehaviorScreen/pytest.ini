# tests/pytest.ini
[pytest]
markers =
    slow: parameter-recovery / multistart tests that fit models with
          self-excitation and/or quadrature-integrated frailty on large
          synthetic datasets. Run `pytest -m "not slow"` for a fast subset.